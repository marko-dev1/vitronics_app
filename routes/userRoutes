const express = require('express');
const router = express.Router();
const userController = require('../controllers/userController');

// Correct route registration
router.post('/register', userController.register); // Must be a function
router.post('/login', userController.login); // Must be a function
router.post('/logout', userController.logout); // Must be a function

module.exports = router;
